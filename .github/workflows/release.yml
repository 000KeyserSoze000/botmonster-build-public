name: 'Tauri Release'

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    # Use minimal permissions for security
    permissions:
      contents: write
      
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install Ubuntu dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.0-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev

      - name: Install Frontend Dependencies (Resilient)
        # Force bash shell on all platforms to ensure syntax compatibility
        shell: bash
        run: |
          # Force the official NPM registry to bypass any corrupted regional mirrors/proxies
          npm config set registry https://registry.npmjs.org/
          # Update NPM to the latest version to fix potential bugs in the version bundled with Node
          npm install -g npm@latest
          # Forcefully clean any residual cache that might exist on the runner
          npm cache clean --force
          # Attempt npm ci first for speed, with a robust npm install fallback for CI flakiness
          npm ci || ( \
            echo "npm ci failed. Cleaning node_modules and retrying with npm install." && \
            rm -rf node_modules && \
            npm install \
          )

      - name: Build and Release Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v2.0.4
          releaseName: "BotMonster v2.0.4"
          releaseBody: "New release of the BotMonster AI Trading Terminal."
          releaseDraft: false
          prerelease: false